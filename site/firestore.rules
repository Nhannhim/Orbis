rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Orbis reads and writes through its authenticated server-side service
    // account. Client-side access stays closed so sessions cannot be queried
    // directly with a copied Firebase project id.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
