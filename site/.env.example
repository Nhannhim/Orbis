# Server-side only. Never prefix this value with NEXT_PUBLIC_.
OPENAI_API_KEY=

# Firebase server credentials. Keep the private key secret and preserve its
# escaped newlines when storing it in a single-line local environment file.
FIREBASE_PROJECT_ID=butler-8deb0
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
